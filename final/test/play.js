const assert = require('assert');
const chai = require('chai');
const Game = require('../class/game/game.js');
const Player = require('../class/player/player.js');
const expect = chai.expect;

describe('main', function () {

    describe('Initiation', function () {

    })

    describe('Play', function () {
        describe('game', function () {

            describe('player', function () {

            })

            describe('dice', function () {

            })



        })
    })

})


class Initiation {
    // 초기화 단계 
    // 이 클래스가 하는 일부터 정리해보자
    // 1. player 2명 세팅
    // 2. 목표점수 세팅
    // 3. 트랩주사위 세팅(0점 되돌아가는 점수)
    // 4. 게임 시작
}
class Play {

    constructor() {

    }
    // 굴린다 { 
    // 1. 주사위 두개 눈 뭐 나왔는지()
    // 2. 1 나온 거 있는지()
    // 3-1. 있으면 점수 0으로 만든다()
    // 3-2. 턴 넘긴다()
    // 4. 1 나온 거 없으면 점수 누적시킨다()
    // }

    // 홀드한다 
    // 턴넘긴다. 

    // Play
    // Game, Player, AimedPointSetter
    // Game : roll, point calc, 



}